'use strict';

const app = require('./src/App')();

const root = document.getElementById('root');


// setTimeout(() => {
//   console.log('setTimeout');
//   root.innerHTML = app;
// }, 1000);
// root.innerHTML = app;
root.innerHTML = app;
// 
